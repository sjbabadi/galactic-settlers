tree("Root")
    while IsMyTurn
        parallel
            mute tree("build")
            mute tree("conquer")

    
tree("conquer")
    while AtLeastOneOfEachBuilding
        while NoUnits
            SpawnUnits


tree("build")
    while not AtLeastOneOfEachBuilding
        sequence
            BuildFarm
            BuildMine
            BuildBarracks
            BuildBarracks
